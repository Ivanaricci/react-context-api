// importo la funzione createContext
import { createContext } from "react";

// utilizzo la funzione per creare il contesto
const PostsContext = CreateContext();

// esportiamo il contesto
export default PostsContext